import { Topic } from '../models/question.model';

export const OOP_TOPIC: Topic = {
  key: 'oop', label: 'OOP & SOLID', icon: '🏗️',
  tip: 'Keep answers short. One-liner + your real project example. No textbook paragraphs.',
  questions: [
    { q: 'What are the 4 pillars of OOP?', tag: 'easy',
      say: 'Encapsulation: hide data, expose only what\'s needed. Employee.Salary is private.\nAbstraction: hide how, show what. Controllers call IPayrollService without knowing EF Core or Dapper.\nInheritance: child reuses parent. All entities inherit BaseEntity with CreatedAt, ModifiedAt.\nPolymorphism: same call, different behavior. ILeaveCalculator.Calculate() differs for annual vs sick leave.',
      note: 'EAIP: Encapsulation, Abstraction, Inheritance, Polymorphism.',
      mem: 'Hide data, hide logic, reuse, same call different behavior.' },
    { q: 'Encapsulation vs Abstraction — trick question', tag: 'med',
      say: 'Encapsulation = hiding DATA. Private fields, class controls its own state.\nAbstraction = hiding COMPLEXITY. Caller knows what to call, not how it works. Interfaces provide abstraction.',
      note: 'Car: gear system hidden inside = encapsulation. Steering wheel as simple interface = abstraction.',
      mem: 'Encapsulation = private fields. Abstraction = interfaces.' },
    { q: 'Abstract class vs Interface?', tag: 'hard',
      say: 'Interface: pure contract, no shared code. Multiple interfaces allowed. Use for capabilities.\nAbstract class: shared code + abstract methods. Single inheritance only. Use when classes share common logic.\n\nSimple rule: no shared code → interface. Shared code → abstract class.',
      note: 'C# 8+: interfaces can have default implementations. Still prefer abstract class for real shared logic.',
      mem: 'Interface = pure contract. Abstract = shared code + forced methods. Interface by default.' },
    { q: 'SOLID — simple explanation', tag: 'med',
      say: 'S: One class, one job.\nO: Add new features with new classes, not editing old ones.\nL: Child class works everywhere parent works.\nI: Don\'t force unused interface methods — split them.\nD: Depend on interfaces, not concrete classes.',
      note: 'Most important in daily work: D — Dependency Inversion. Foundation of DI containers.',
      mem: 'S=one job, O=extend not modify, L=substitutable, I=small interfaces, D=use abstractions.' },
    { q: 'override vs new keyword in C#?', tag: 'hard',
      say: 'override: properly replaces parent\'s virtual method. Child\'s version runs even when called through parent-type variable. Real polymorphism.\n\nnew: hides parent method but doesn\'t replace it. Parent-type variable still calls parent\'s version. Confusing, usually a bug.\n\nAlways use override.',
      note: 'Animal a = new Dog();\noverride → Dog\'s method runs.\nnew → Animal\'s method runs (wrong!)',
      mem: 'override = real polymorphism. new = method hiding, usually a mistake.' },
    { q: 'Why is _balance private and not public?', tag: 'easy',
      say: 'If it were public, anyone could set balance to -99999 directly. Making it private forces all changes through Deposit/Withdraw where validation runs. That\'s encapsulation — protect the state, control the access.',
      note: 'Encapsulation benefit: internal implementation can change without breaking callers.',
      mem: 'Private = protect state. Force all changes through validated methods.' },
    { q: 'When would you use inheritance vs composition?', tag: 'med',
      say: 'Inheritance when there\'s a true "is-a" — Manager IS an Employee. Composition when it\'s "has-a" — an Order HAS a Payment. Composition is more flexible; inheritance creates tight coupling. Rule of thumb: prefer composition, use inheritance only when the relationship is genuinely hierarchical.',
      note: '"Prefer composition over inheritance" is a classic GoF principle.',
      mem: 'is-a = inheritance. has-a = composition. Prefer composition.' },
    { q: 'How does abstraction connect to Dependency Injection?', tag: 'med',
      say: 'OrderService depends on IPaymentGateway, not a concrete class. In ASP.NET Core you register: services.AddScoped<IPaymentGateway, StripeGateway>(). At runtime ASP.NET Core injects the right implementation. You can swap Stripe for Razorpay without touching OrderService — abstraction + DI working together.',
      note: 'Connecting abstraction to DI shows senior-level thinking. Most juniors answer these as separate topics.',
      mem: 'Interface + DI = swap implementations without touching business logic.' },
  ]
};
